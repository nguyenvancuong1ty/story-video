export const ArtifactPanel = ({ stage }: { stage?: { key: string; status: string; log?: string; costUsd?: number; warning?: string } }) => <aside className="artifact-panel"><p className="eyebrow">Selected Stage</p><h2>{stage?.key?.replaceAll("_", " ") ?? "Select a stage"}</h2><p className="stage-status">{stage?.status ?? "No selection"}</p><dl><div><dt>Cost</dt><dd>${stage?.costUsd?.toFixed(2) ?? "0.00"}</dd></div><div><dt>Log</dt><dd>{stage?.log ?? "Choose a workflow node to inspect its artifacts."}</dd></div>{stage?.warning ? <div><dt>Warning</dt><dd>{stage.warning}</dd></div> : null}</dl></aside>;
